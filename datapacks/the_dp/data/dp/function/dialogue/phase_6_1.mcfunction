function dp:dialogue/ding
execute as @r run tellraw @a ["\n<",{selector:"@s"},">: but looks like there's only one canister of it, so we must ",{color:"red",bold:true,italic:true,underlined:true,text:"manage our"},{color:"gold",bold:true,italic:true,underlined:true,text:" resources"}]
